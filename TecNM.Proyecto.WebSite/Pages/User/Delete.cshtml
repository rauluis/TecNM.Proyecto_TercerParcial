@page "{id:int}"
@model TecNM.Proyecto.WebSite.Pages.User.Delete

<div class="container" style="margin-bottom: 200px;">
    <div class="row">
        <div class="col-md-10 mt-10">
            <!-- Contenido del primer div -->
        </div>
    </div>
    <div class="row">
        <div class="col-md-10 mb-10">
            <!-- Contenido del segundo div -->
        </div>
    </div>
</div>
<div class="container-fluid">
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <h2>Borrar Usuario</h2>

            <form method="post">
                <input type="hidden" asp-for="User.Id"/>
                <div class="form-group">
                    <label asp-for="User.Name"></label>
                    <input asp-for="User.Name" class="form-control" maxlength="45"/>
                    <span class="text-danger" asp-validation-for="User.Name"></span>
                </div>

                <div class="form-group">
                    <label asp-for="User.Username"></label>
                    <input asp-for="User.Username" class="form-control" maxlength="100"/>
                    <span class="text-danger" asp-validation-for="User.Username"></span>
                </div>
                <div class="form-group">
                    <label asp-for="User.Address"></label>
                    <input asp-for="User.Address" class="form-control" maxlength="100"/>
                    <span class="text-danger" asp-validation-for="User.Address"></span>
                </div>
                <div class="form-group">
                    <label asp-for="User.Phone"></label>
                    <input asp-for="User.Phone" class="form-control" maxlength="100"/>
                    <span class="text-danger" asp-validation-for="User.Phone"></span>
                </div>

                <div class="col-md-1"></div>
                @if (Model.Errors.Count > 0)
                {
                    <div class="alert alert-danger" role="alert">
                        <ul>
                            @foreach (var error in Model.Errors)
                            {
                                <li>Ocurrio un error</li>
                            }
                        </ul>
                    </div>
                }

                <div class="col-md-12">
                    <div class="mb-4"></div>
                </div>
                <div class="row">
                    <div class="col-md-1"></div>
                    <div class="col-md-12 text-right">
                        <button type="submit" class="btn btn-danger mr-3">Borrar</button>
                        <a class="btn btn-secondary" asp-page="/User/List">Cancelar</a>
                    </div>
                </div>
            </form>
        </div>
        <div class="col-md-1"></div>
    </div>
</div>

<div class="container" style="margin-bottom: 100px;">
    <div class="row">
        <div class="col-md-10 mt-10">
            <!-- Contenido del primer div -->
        </div>
    </div>
    <div class="row">
        <div class="col-md-10 mb-10">
            <!-- Contenido del segundo div -->
        </div>
    </div>
</div>